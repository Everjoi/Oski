@* @model Test

<!DOCTYPE html>
<html>
<head>
    <title>Система тестування</title>
</head>
<body>

    <h2>Вітаємо, @Model.Username!</h2>

    <h3>Доступні тести:</h3>
    <table border="1">
        <thead>
            <tr>
                <th>Назва тесту</th>
                <th>Статус</th>
                <th>Оцінка</th>
                <th>Дія</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var test in Model.AvailableTests)
            {
                <tr>
                    <td>@test.TestName</td>
                    <td>@(test.IsCompleted ? "Завершено" : "Доступний до проходження")</td>
                    <td>@(test.IsCompleted ? test.Score.ToString() : "N/A")</td>
                    <td>
                        @if(!test.IsCompleted)
                        {
                            <a href="@Url.Action("TakeTest", "Test", new { testId = test.TestId })">Пройти тест</a>
                        }
                        else
                        {
                            <span>Тест завершено</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>

</body>
</html>

 *@